
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@using PagedList.Mvc;
@model PagedList.IPagedList<BookShop.Models.LinhVuc>
<div class="x_panel">
    <div class="x_title">
        <h2>Danh sách lĩnh vực </h2>
        <ul class="nav navbar-right panel_toolbox">
            <li>
                <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            </li>
            <li>
                <a class="close-link"><i class="fa fa-close"></i></a>
            </li>
        </ul>
        <div class="clearfix"></div>
    </div>
    <div class="x_content">
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Mã lĩnh vực</th>
                    <th>Tên lĩnh vực</th>
                    <th>Ngày chỉnh sửa</th>
                    <th>Tác vụ</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count; i++)
                {
                    var item = Model.ElementAt(i);
                    var index = (ViewBag.page) * 10 - (10 - i) + 1;
                <tr>
                    <th scope="row">@index</th>
                    <td>@item.MaLinhVuc</td>
                    <td>@item.TenLinhVuc</td>
                    <td>@item.NgaySua</td>
                    <td>
                        <li role="presentation" class="dropdown">
                            <a id="drop6" href="#" class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" role="button" aria-expanded="true">
                                Chọn tác vụ
                                <span class="caret"></span>
                            </a>
                            <ul id="menu3" class="dropdown-menu animated fadeInDown" role="menu" aria-labelledby="drop6">
                                <li role="presentation" class="divider"></li>
                                <li role="presentation">
                                    <a role="menuitem" tabindex="-1" href="@Url.Action("Details", "Category", new { id = item.MaLinhVuc.TrimEnd()})">Chi tiết</a>
                                </li>
                                <li role="presentation">
                                    <a role="menuitem" tabindex="-1" href="@Url.Action("Edit", "Category", new { id = item.MaLinhVuc.TrimEnd()})">Sửa</a>
                                </li>
                                <li role="presentation" class="divider"></li>
                                <li role="presentation">
                                    <a role="menuitem" tabindex="-1" href="@Url.Action("Delete", "Category", new { id = item.MaLinhVuc.TrimEnd()})">Xóa</a>
                                </li>
                            </ul>
                        </li>
                    </td>
                </tr>
                }
            </tbody>
        </table>
        <div>
            @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
        </div>
    </div>
</div>

